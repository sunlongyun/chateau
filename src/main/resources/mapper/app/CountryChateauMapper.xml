<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caichao.chateau.app.mapper.CountryChateauMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.caichao.chateau.app.entity.CountryChateau">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="code" property="code" />
        <result column="pic_url" property="picUrl" />
        <result column="min_pic_url" property="minPicUrl" />
        <result column="video" property="video" />
        <result column="daily_broadcast_ing" property="dailyBroadcastIng" />
        <result column="master_broadcast_ing" property="masterBroadcastIng" />
        <result column="daily_broadcast" property="dailyBroadcast" />
        <result column="master_broadcast" property="masterBroadcast" />
        <result column="country_id" property="countryId" />
        <result column="country_name" property="countryName" />
        <result column="postage" property="postage" />
        <result column="description" property="description" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="validity" property="validity" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, code, pic_url, min_pic_url, video, daily_broadcast_ing, master_broadcast_ing, daily_broadcast, master_broadcast, country_id, country_name, postage, description, create_time, update_time, validity
    </sql>



    <!-- example通用查询-->
    <select id="selectByExample" parameterType="com.caichao.chateau.app.example.CountryChateauExample" resultMap="BaseResultMap">
        select
        <if test="distinct">
            distinct
        </if>
        'false' as QUERYID,
        <include refid="Base_Column_List" />
        from country_chateau
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>

    <!-- 条件-->
    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                    #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>
</mapper>
